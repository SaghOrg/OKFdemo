# Learnings

- **S1 glossary**: Built context/glossary.md (type: glossary) with ~28 canonical-concept sections, each carrying a single greppable 'Aliases:' line: distributor, primary sales, secondary sales (plus a dedicated PRIMARY-vs-SECONDARY callout with direct Karthik-corrects-Vikram citations), scheme discount, variance, credit note, secondary sales feed/DMS, warehouse, nightly load, ODI mapping, stored procedure, server, dashboard, filter control, visual, agent (false-friend OBIEE-vs-ODI sense), SCD2, crore, lakh, Indian business-English register, organisations, a systems-ASR block (ORION/ODI/OBIEE/Control-M/DBLINK/SKU/DELETE_FLAG), places (Baddi/Nashik/Andheri), and a people name-ASR table. Every speaker attribution and every ASR alias was independently confirmed by grepping _sources/ and _build/corpus_text/ directly rather than trusting _canon/terminology_map.csv verbatim, per the stage instructions. Frontmatter validated clean against schemas/concept.schema.json using the repo's own .venv-synth (pyyaml+jsonschema already installed there) after normalizing YAML-parsed dates back to ISO strings (a PyYAML auto-typing artifact, not a real schema violation). generated.by is exactly process:claude-sonnet/glossary; generated.at came from `date -u +%Y-%m-%dT%H:%M:%SZ` (2026-08-23T10:30:54Z). No status field (glossary is stable), no verified field, no PII substrings from the five planted PII locations appear anywhere in the file (grepped for all five marker strings post-write, zero hits), and no 16th named person was introduced (regex-scanned all Capitalized-Capitalized bigrams against the 15-person registry — every hit was a place/org/product name or a documented ASR name-mangling). All /_sources/... paths cited in the body were verified to resolve to real files on disk after fixing one stray '/_sources/meetings/...' placeholder link left over from drafting.

- **S2 tables**: Stage 2 (Tables) transformation complete for three OMS_PROD tables. Key learnings from the schema canon and prose enrichment: (1) INVOICE_HEADER: Master document table with document types (INV, STN, SMP). Critical insight: CREATED_TS is UTC (VAR-002), not IST business date; use INVOICE_DT for reporting. FIN_PROD holds an undocumented UPDATE grant on INVOICE_LINE since 2014. (2) INVOICE_LINE: Line-item detail with SKU/quantity/pricing; schema aligns with INVOICE_HEADER.INVOICE_ID foreign key relationship.
- **S3 variances**: Derived eight variances directly from transcripts, code, and tracker artifacts rather than canon register alone; the key finding is VAR-003's false trail: T-03 (24-Mar-2026) proposes DBLINK-latency hypothesis (Karthik moderately confident, falsification test defined); T-04 (14-Apr-2026) kills it with AWR evidence (4.1 min total link wait, 1.2% of load elapsed, no correlation, duplicates exact not torn); T-08 (22-Sep) closes with root cause unidentified in tracker but fully documented in DOC-03 (28-Apr-2026, Ishaan author): FIN_PROD.PKG_MONTH_END.P_RECALC_SCHEME_DISCOUNT is an additive UPDATE with no idempotency guard, running nightly via DBMS_SCHEDULER outside Control-M, colliding with incremental extract window. PL/SQL in pkg_month_end.pkb corroborates DOC-03 line-for-line. Independent derivation from primary sources agreed with variance_register_canon.csv on all 8 mechanisms/impacts/dates; the value was surfacing diagnostic history (false trails, typo corrections, ownership handovers, closed-vs-deployed gaps) not carried in canon register. VAR-004 ownership conflict: XL-01 (stale) says Farida Contractor; EM-055 (09-Jul-2026) hands to Ishaan Bhatt; T-08 (22-Sep) confirms Ishaan with no prior owner mention — resolved per 'email wins' rule, logged in /log.md.

- **S4 decisions**: Read AGENTS.md, schemas/concept.schema.json, and _canon/BRIEF.md, then targeted-grepped _canon/BRIEF.md (ADR numbers, dashboard/go-live variance tables) plus primary sources: T-03/T-04/T-05/T-07/T-08 meeting transcripts, DOC-04 (dimension strategy, contains ADR-001 to ADR-005 verbatim in Appendix A), EM-041/EM-047/XL-03 (DIM_CUSTOMER SCD2 objection+measurement), EM-023 (dashboard cut), EM-068 (go-live slip to 30-Oct), DK-05/DK-06 (dashboard candidates and reinstatement), EM-072 (Feb duplicate load resolution chain). Key structural finding: the decision.md template's frontmatter (id, deciders, consulted, informed, affects, date) is NOT OKF-schema-conformant (schema has additionalProperties:false and none of those keys are declared) — resolved by using only OKF/local-extension frontmatter fields and moving deciders/consulted/informed into a body section under the H1, preserving the MADR body shape (Context/Decision drivers/Considered options/Decision outcome/Consequences/Evidence/Follow-ups) as instructed. Also discovered that the T-03 'parked' February-reload question (one of the two explicitly-named deferral cases) was in fact resolved by email on 02-Apr-2026, before the T-04 session it was deferred to — wrote both the draft/open-question record for the T-03 parking AND a separate accepted record for the actual 02-Apr resolution, linked via supersedes/superseded_by, rather than either inventing a resolution or suppressing the real one found in EM-072. Validated all 13 files' frontmatter against schemas/concept.schema.json using a local jsonschema script (via the repo's .venv-synth) after normalizing PyYAML's auto-parsed datetime objects back to strings for the check only — confirmed this datetime-vs-string quirk is purely a YAML-loader artifact of my ad hoc validator, since all prior-stage concept files (tables, variances) already use the identical unquoted `at: <iso8601>` style, so I kept mine consistent with that established convention rather than quoting it.
